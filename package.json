{
  "name": "astro-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "aws-deploy": "AWS_MAX_CONCURRENT_REQUESTS=50 aws s3 cp dist/ s3://westack-it-eu/ --recursive --endpoint-url https://luxembourg-2.storage.gcore.dev",
    "fe:build": "fastedge-build --config .fastedge/build-config.js",
    "fe:run": "npm run fe:build && fastedge-run http -w .fastedge/fastedge.wasm --port 8080 --wasi-http true --dotenv .env"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "astro": "^5.12.9",
    "astro-compress": "^2.3.8"
  },
  "devDependencies": {
    "@gcoredev/fastedge-sdk-js": "^2.5.1",
    "glob": "^11.0.3",
    "sass": "^1.91.0",
    "sharp": "^0.34.3",
    "vite-plugin-style-import": "^2.0.0"
  }
}
