#!/bin/bash

// So, the purpose is to unzip the assets according to windows/macos/linux
// builds and then test the edge runtime
// https://github.com/G-Core/FastEdge-lib/releases/tag/v0.20.0 (github releases)
//
// first detect the platform and then unzip the assets to the a folder that is always available in the PATH
//
