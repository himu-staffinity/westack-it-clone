---
import { Image } from "astro:assets";
import logo from "../assets/images/logo.svg";
import { COMPANY_NAME } from "../config";
import { authors } from "../data/authors.js";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import MultiLineText from "./i18n/MultiLineText.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const companyAddress = t("footer.company_address");
const privacyHref = lang === "de" ? "/privacy-policy/" : "/en/privacy-policy/";
---

<footer class="relative overflow-hidden border-t border-fg/10 bg-base">
  <section id="footer" class="relative">
    <div class="bg-grid absolute inset-0 opacity-40" aria-hidden="true"></div>

    <div class="relative z-10 mx-auto max-w-7xl px-5 pt-24 lg:px-8">
      <p class="reveal text-center font-mono text-[11px] uppercase tracking-[0.3em] text-faint">
        {t("site.description")}
      </p>
      <div
        class="giant-word reveal mt-6 select-none"
        aria-hidden="true"
      >
        WeStack
      </div>

      <div class="reveal mt-20 grid gap-14 md:grid-cols-2 lg:grid-cols-[1.3fr_1fr_1.2fr]">
        <div>
          <div class="flex items-center gap-2.5">
            <Image src={logo} alt="" width={28} height={28} class="h-7 w-7" />
            <span class="font-display text-lg font-semibold tracking-tight text-fg">
              WeStack
            </span>
          </div>
          <p class="mt-5 max-w-xs text-sm leading-relaxed text-muted">
            {t("site.description")}
          </p>
          <div class="mt-7 border-l border-accent/30 pl-4">
            <MultiLineText
              lines={companyAddress}
              tag="p"
              class="company-address text-xs leading-relaxed text-faint"
            />
            <p class="mt-2 text-xs text-faint">{COMPANY_NAME}</p>
          </div>
        </div>

        <div>
          <h3 class="font-mono text-[11px] uppercase tracking-[0.28em] text-accent">
            {t("footer.contact")}
          </h3>
          <div class="mt-5 space-y-5">
            <div>
              <p class="font-mono text-[10px] uppercase tracking-widest text-faint">
                {t("footer.phone")}
              </p>
              <a
                href="tel:+4991174393000"
                class="mt-1 inline-block text-sm text-fg transition-colors hover:text-accent"
              >
                +49&nbsp;911&nbsp;743&nbsp;930&nbsp;00
              </a>
            </div>
            <div>
              <p class="font-mono text-[10px] uppercase tracking-widest text-faint">
                {t("footer.email")}
              </p>
              <a
                href="mailto:info@westack-it.eu"
                class="mt-1 inline-block text-sm text-fg transition-colors hover:text-accent"
              >
                info@westack-it.eu
              </a>
            </div>
            <div>
              <p class="font-mono text-[10px] uppercase tracking-widest text-faint">
                {t("footer.imprint")}
              </p>
              <p class="mt-1 text-sm text-fg">{COMPANY_NAME}</p>
            </div>
          </div>
        </div>

        <div>
          <h3 class="font-mono text-[11px] uppercase tracking-[0.28em] text-accent">
            {lang === "de" ? "Das Team" : "The Team"}
          </h3>
          <ul class="mt-5 space-y-5">
            {authors.map((author) => (
              <li class="group flex items-start gap-4">
                <Image
                  src={author.image}
                  alt={author.name}
                  widths={[64, 128, 180]}
                  sizes="64px"
                  height={64}
                  width={64}
                  format="webp"
                  class="h-14 w-14 shrink-0 rounded-full object-cover ring-1 ring-fg/15 transition-all duration-300 group-hover:scale-105 group-hover:ring-accent/60"
                />
                <div>
                  <p class="text-sm font-medium text-fg">{author.name}</p>
                  <p class="mt-0.5 text-xs text-muted">
                    {author.title} · {author.role}
                  </p>
                  <a
                    href={`mailto:${author.email}`}
                    class="mt-1 inline-block font-mono text-[11px] text-accent transition-colors hover:text-fg"
                  >
                    {author.email}
                  </a>
                </div>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <div class="mt-16 flex flex-col items-center justify-between gap-4 border-t border-fg/10 py-8 sm:flex-row">
        <p class="text-xs text-faint">© {new Date().getFullYear()} {COMPANY_NAME}</p>
        <a
          href={privacyHref}
          class="font-mono text-[11px] uppercase tracking-widest text-faint transition-colors hover:text-accent"
        >
          {t("footer.privacy")}
        </a>
      </div>
    </div>
  </section>
</footer>
